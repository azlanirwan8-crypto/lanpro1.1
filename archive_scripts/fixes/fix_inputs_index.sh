sed -i -e 's/<input \n                    type="number"\n                    disabled={!isEditable}\n                    value={task.storyPoints || '"'"''"'"'}\n                    onChange={(e) => updateTaskField(task.id, '"'"'storyPoints'"'"', e.target.value ? Number(e.target.value) : null)}/<input \n                    type="number"\n                    disabled={!isEditable}\n                    defaultValue={task.storyPoints || '"'"''"'"'}\n                    onBlur={(e) => updateTaskField(task.id, '"'"'storyPoints'"'"', e.target.value ? Number(e.target.value) : null)}/g' src/features/issues/index.tsx
