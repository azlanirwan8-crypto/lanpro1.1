sed -i -e 's/<input \n                    type="date"\n                    disabled={!isEditable}\n                    value={dateVal ? format(ensureDate(dateVal), '"'"'yyyy-MM-dd'"'"') : '"'"''"'"'}\n                    onChange={(e) => updateTaskField(task.id, col.id, e.target.value)}/<input \n                    type="date"\n                    disabled={!isEditable}\n                    defaultValue={dateVal ? format(ensureDate(dateVal), '"'"'yyyy-MM-dd'"'"') : '"'"''"'"'}\n                    onBlur={(e) => updateTaskField(task.id, col.id, e.target.value)}/g' src/features/issues/index.tsx
