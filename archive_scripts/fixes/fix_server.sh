sed -i -e "s/const upload = multer({ dest: process.env.VERCEL ? '\/tmp\/uploads\/' : 'uploads\/' });/const isServerless = !!process.env.VERCEL || !!process.env.AWS_EXECUTION_ENV || process.cwd() === '\/var\/task' || process.cwd().includes('\/var\/task');\nconst GLOBAL_UPLOADS_DIR = isServerless ? '\/tmp\/uploads' : path.join(process.cwd(), 'uploads');\nconst upload = multer({ dest: GLOBAL_UPLOADS_DIR });/g" server.ts
